#цель: зависимость
#[tab] команда


BIN = UDP_listen
SOURCES = main.c
HEADERS = main.h
CC := gcc

all:
	$(CC) $(SOURCES) -o $(BIN).out
$(BIN): $(HEADERS)

run: all
	./$(BIN).out

run2:
	./$(BIN).out
#clean:
#	rm -rf $(BIN) *.o $(BIN).out



















